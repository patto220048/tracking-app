# Mobile App

Thư mục này sẽ chứa mã nguồn UI của ứng dụng trên Android và iOS.

*Ghi chú: Tại đây bạn có thể chạy lệnh `npx react-native init mobile_app` (đối với React Native) hoặc `flutter create .` (đối với Flutter) để bắt đầu khởi tạo dự án thực tế.*
